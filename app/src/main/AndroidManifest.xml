<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.itesm.thegoldenbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.GaleriaLibros"></activity>
        <activity android:name=".ui.activities.Ayuda" />
        <activity android:name=".ui.activities.InfoUsuario" />
        <activity android:name=".ui.activities.VisualizarEsteLibro" />
        <activity android:name=".ui.activities.VisualizarLibros" />
        <activity android:name=".ui.activities.EditarEsteLibro" />
        <activity android:name=".ui.activities.EditarLibro" />
        <activity android:name=".ui.activities.CrearLibro" />
        <activity android:name=".ui.activities.ActividadMenu2" />
        <activity android:name=".ui.activities.ActividadCrearCuenta" />
        <activity android:name=".ui.activities.ActividadLogin" />
        <activity android:name=".ui.activities.ActividadMenu1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>