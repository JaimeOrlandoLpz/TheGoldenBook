<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.itesm.Legacy.TheGoldenBook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GaleriaLibros"></activity>
        <activity android:name=".Ayuda" />
        <activity android:name=".InfoUsuario" />
        <activity android:name=".VisualizarEsteLibro" />
        <activity android:name=".VisualizarLibros" />
        <activity android:name=".EditarEsteLibro" />
        <activity android:name=".EditarLibro" />
        <activity android:name=".CrearLibro" />
        <activity android:name=".ActividadMenu2" />
        <activity android:name=".ActividadCrearCuenta" />
        <activity android:name=".ActividadLogin" />
        <activity android:name=".ActividadMenu1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>